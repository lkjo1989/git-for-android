# Robolectric configuration for GitForAndroid Compose UI tests
# Runs on host JVM — no device/emulator needed

# Android SDK version to emulate
sdk=33

# The merged AndroidManifest.xml is copied from the build output to
# src/test/resources/ so Robolectric finds it on the test classpath.
# No explicit manifest path needed — auto-discovery from classpath.
